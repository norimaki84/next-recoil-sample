import { Fragment } from 'react'
// type FormatStringDateType = string
// TODO：未完成、使わないかも
export type Props = {
  text?: string
}
//
// const convertNewline = ({ data }: Props) => {
//   // const outputTextVal: string = data.replace(/\n/g, '<br />')
//
//   data.split('\n').map((str, index) => {
//     const last = data.split('\n').length - 1
//     return (
//       <Fragment key={index}>
//         {str}
//         {index !== last ? <br /> : null}
//       </Fragment>
//     )
//   })
// }
//
// export { convertNewline }
